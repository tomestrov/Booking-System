#include "booking.h" 
#include "flight.h"
#include "lodging.h"
#include "trip.h"

int main() {
    using namespace std;
    Booking booking_program;
    booking_program.start();
    // Flight f1 = Flight("new york", "tokyo", 2, 9);
    // cout << f1.getFlightCost() << endl;
    // Flight f2 = Flight("paris", "london", 3, 90);
    // cout << f2.getFlightCost() << endl;
    return 0;
}